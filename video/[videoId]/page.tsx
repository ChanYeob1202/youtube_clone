import React from 'react'

interface VideoPageProps {
  params: {
    vidoeId: string;
  }
}

// import api videoDetail

function page({params}: VideoPageProps) {
  return (
    <div>
      
    </div>
  )
}

export default page
